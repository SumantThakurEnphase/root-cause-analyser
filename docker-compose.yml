version: "3.8"

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: rca-chromadb
    ports:
      - "8000:8000"
    volumes:
      - chromadb_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE

volumes:
  chromadb_data:
